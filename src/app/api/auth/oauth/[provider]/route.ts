import { NextResponse } from "next/server";
import { authorizationUrl, createOAuthState, isProvider } from "@/lib/oauth";
export async function GET(_request: Request, { params }: { params: Promise<{ provider: string }> }) { const { provider } = await params; if (!isProvider(provider)) return NextResponse.json({ error: "Unsupported provider" }, { status: 404 }); try { const state = createOAuthState(); const response = NextResponse.redirect(authorizationUrl(provider, state)); const isApple = provider === "apple"; response.cookies.set("oauth_state", state, { httpOnly: true, sameSite: isApple ? "none" : "lax", secure: isApple || process.env.NODE_ENV === "production", maxAge: 600, path: "/" }); return response; } catch (error) { const message = error instanceof Error ? error.message : "OAuth is not configured."; return NextResponse.redirect(new URL(`/login?oauthError=${encodeURIComponent(message)}`, process.env.APP_URL ?? "http://localhost:3000")); } }
